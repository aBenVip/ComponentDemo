package com.dgcredit.httplib;

/**
 * 类描述:
 * 创建人:aBen
 * 创建时间:2017/6/26
 * 备注:
 */

public interface CacheProvider {
/*    @LifeCache(duration = 3000,timeUnit = TimeUnit.MINUTES)
    Observable<String> getData(Observable<String> observer, EvictProvider provider);
    @LifeCache(duration = 3000,timeUnit = TimeUnit.MINUTES)
    Observable<ResponseBody> getData1(Observable<ResponseBody> observer, EvictProvider provider);*/
}
